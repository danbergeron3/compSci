#ifndef STATUS_H
#define STATUS_H

typedef enum status { FAILURE, SUCCESS } Status;

enum boolean {
	FALSE,
	TRUE
};

typedef enum boolean Boolean;

typedef enum state {
	LEFT,
	RIGHT
}State;


#endif
